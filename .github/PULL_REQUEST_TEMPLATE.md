## What & why

<!-- One or two sentences. What changed, and why it's needed. -->

## Checklist

- [ ] `swift build` and `swift build -c release` both succeed locally
- [ ] Manually ran the widget (`./build-app.sh && open Overcast.app`) and confirmed the change actually works — not just "it compiles"
- [ ] No leftover `TODO`/debug prints
- [ ] Scoped to one thing (split unrelated changes into separate PRs)
